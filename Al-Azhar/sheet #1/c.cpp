#include <iostream>
int main() {
  int x, y;
  std::cin >> x >> y;
  std::cout << x << " + " << y << " = " << x + y << "\n";
  std::cout << x << " * " << y << " = " << (long long)x * (long long)y << "\n";
  std::cout << x << " - " << y << " = " << x - y << "\n";
  return 0;
}
